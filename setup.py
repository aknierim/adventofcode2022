from setuptools import setup, find_packages

setup(
    name='aoctools',
    version='0.1.0',
    packages=find_packages(),
    author='Anno Knierim',
    install_requires=[
        'requests',
    ]
)